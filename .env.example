# Local development only. Windows production paths are resolved from LOCALAPPDATA.
LAS_ENV=development
LAS_HOST=127.0.0.1
LAS_PORT=8765
# Production automatically uses %LOCALAPPDATA%\LocalAgentStudio.
# Development-only secret; never use or commit a real value.
# LAS_DEV_SECRET=replace-with-a-random-local-development-secret
