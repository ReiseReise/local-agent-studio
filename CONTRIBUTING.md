# Contributing

1. Open an issue describing the user-visible problem.
2. Create a focused branch and include tests.
3. Run `python -m pytest -q`, `ruff check .` and `python scripts/check_public_repo.py`.
4. Never include real contacts, chat text, private prompts, knowledge files, credentials or activation material.
5. Contributions are accepted under Apache-2.0.
